//AcWing
#include<iostream>

using namespace std;

const int N = 1000010;

int n;
int e[N], ne[N], head, idx;

void init()
{
    head = 0, ne[head] = -1, idx = 1;
}

void add_to_head(int x)
{
    e[idx] = x, ne[idx] = ne[head], ne[head] = idx++;
}

void add(int k, int x)
{
    e[idx] = x, ne[idx] = ne[k], ne[k] = idx++;
}

void remove(int k)
{
    ne[k] = ne[ne[k]];
}

int main()
{
    cin >> n;
    
    init();
    
    int k, x;
    char ch;
    while (n--) {
        while ((ch = getchar()) == '\n');
        switch (ch) {
            case 'H':
                cin >> x;
                add_to_head(x);
                break;
            case 'D':
                cin >> k;
                remove(k);
                break;
            case 'I':
                cin >> k >> x;
                add(k, x);
                break;
        }
    }
    
    for (int i = ne[head]; i != -1; i = ne[i]) printf("%d ", e[i]);
    
    return 0;
}
