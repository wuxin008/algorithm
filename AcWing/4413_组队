//AcWing
#include<iostream>

using namespace std;

const int N = 2010;

int n, k;
int a[N];

int main()
{
    cin >> n >> k;
    
    int res = 0;
    for (int i = 1; i <= n; ++i) {
        cin >> a[i];
        if (a[i] <= 5 - k) res++;
    }
    
    cout << res / 3 << endl;
    
    return 0;
}
