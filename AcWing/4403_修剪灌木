//AcWing
#include<iostream>

using namespace std;

int main()
{
    int n;
    cin >> n;
    
    for (int i = 1; i <= n; ++i) {
        printf("%d\n", max(abs(i - 1) * 2, abs(i - n) * 2));
    }
    
    return 0;
}
